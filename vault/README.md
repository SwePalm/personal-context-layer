# Development vault

This directory is intentionally empty in the product repository. Personal use must set `PERSONAL_CONTEXT_ROOT` to a separate private vault repository; the deployment command configures this automatically.

Tests create isolated temporary vaults and must not write personal context here.
